-- =============================================
-- INFINITY UI v2.0 - Rayfield Style
-- Feito pra ficar igual Rayfield + mobile
-- Animação de entrada com nome INFINITY UI
-- Autor: B13 Coringa
-- =============================================

local InfinityUI = {}
InfinityUI.__index = InfinityUI

-- ==================== TEMA RAYFIELD STYLE ====================
InfinityUI.Theme = {
    Background   = Color3.fromRGB(15, 15, 20),
    TitleBar     = Color3.fromRGB(10, 10, 15),
    Accent       = Color3.fromRGB(0, 255, 200),     -- neon ciano (igual Rayfield)
    Button       = Color3.fromRGB(30, 30, 40),
    ButtonHover  = Color3.fromRGB(45, 45, 60),
    Text         = Color3.fromRGB(255, 255, 255),
    Border       = Color3.fromRGB(50, 50, 70),
    SliderTrack  = Color3.fromRGB(25, 25, 35),
}

-- ==================== CRIAR BIBLIOTECA ====================
function InfinityUI.new()
    local self = setmetatable({}, InfinityUI)

    local success, hui = pcall(function() return gethui() end)
    self.ScreenGui = Instance.new("ScreenGui")
    self.ScreenGui.Name = "InfinityUI_By_B13_Coringa"
    self.ScreenGui.ResetOnSpawn = false
    self.ScreenGui.Parent = success and hui or game.Players.LocalPlayer:WaitForChild("PlayerGui")

    self.Windows = {}
    return self
end

-- ==================== CRIAR JANELA ====================
function InfinityUI:CreateWindow(options)
    options = options or {}

    local window = {}
    window.Tabs = {}
    window.CurrentTab = nil

    window.Frame = Instance.new("Frame")
    window.Frame.Size = UDim2.new(0, 420, 0, 520)
    window.Frame.Position = UDim2.new(0.5, -210, 0.5, -260)
    window.Frame.BackgroundColor3 = options.BackgroundColor or self.Theme.Background
    window.Frame.BackgroundTransparency = 1
    window.Frame.BorderSizePixel = 0
    window.Frame.Parent = self.ScreenGui

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 18)
    corner.Parent = window.Frame

    local stroke = Instance.new("UIStroke")
    stroke.Color = self.Theme.Border
    stroke.Thickness = 2.5
    stroke.Parent = window.Frame

    -- ==================== ANIMAÇÃO DE ENTRADA ====================
    local TweenService = game:GetService("TweenService")
    window.Frame.Size = UDim2.new(0, 380, 0, 460)
    local enterTween = TweenService:Create(window.Frame, TweenInfo.new(0.6, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        BackgroundTransparency = 0,
        Size = UDim2.new(0, 420, 0, 520)
    })
    enterTween:Play()

    -- ==================== BARRA DE TÍTULO COM NOME INFINITY UI ====================
    local titleBar = Instance.new("Frame")
    titleBar.Size = UDim2.new(1, 0, 0, 60)
    titleBar.BackgroundColor3 = self.Theme.TitleBar
    titleBar.BorderSizePixel = 0
    titleBar.Parent = window.Frame

    local titleCorner = Instance.new("UICorner")
    titleCorner.CornerRadius = UDim.new(0, 18)
    titleCorner.Parent = titleBar

    local titleLabel = Instance.new("TextLabel")
    titleLabel.Size = UDim2.new(1, 0, 1, 0)
    titleLabel.BackgroundTransparency = 1
    titleLabel.Text = "∞ INFINITY UI"
    titleLabel.TextColor3 = self.Theme.Accent
    titleLabel.TextSize = 24
    titleLabel.Font = Enum.Font.GothamBold
    titleLabel.Parent = titleBar

    -- Efeito de brilho no título (Rayfield style)
    local glow = Instance.new("UIStroke")
    glow.Color = self.Theme.Accent
    glow.Thickness = 1.5
    glow.Transparency = 0.7
    glow.Parent = titleLabel

    local authorLabel = Instance.new("TextLabel")
    authorLabel.Size = UDim2.new(0, 100, 0, 20)
    authorLabel.Position = UDim2.new(1, -110, 0, 8)
    authorLabel.BackgroundTransparency = 1
    authorLabel.Text = "by B13 Coringa"
    authorLabel.TextColor3 = Color3.fromRGB(140, 140, 140)
    authorLabel.TextSize = 13
    authorLabel.Font = Enum.Font.Gotham
    authorLabel.Parent = titleBar

    -- Botão fechar
    local closeBtn = Instance.new("TextButton")
    closeBtn.Size = UDim2.new(0, 40, 0, 40)
    closeBtn.Position = UDim2.new(1, -48, 0, 10)
    closeBtn.BackgroundTransparency = 1
    closeBtn.Text = "✕"
    closeBtn.TextColor3 = Color3.fromRGB(255, 90, 90)
    closeBtn.TextSize = 26
    closeBtn.Font = Enum.Font.GothamBold
    closeBtn.Parent = titleBar
    closeBtn.MouseButton1Click:Connect(function()
        window.Frame:Destroy()
    end)

    -- ==================== RESTO DO CÓDIGO (tabs, botões, etc.) ====================
    -- (mantive a mesma estrutura fácil, só melhorei o visual)

    local tabBar = Instance.new("Frame")
    tabBar.Size = UDim2.new(1, 0, 0, 50)
    tabBar.Position = UDim2.new(0, 0, 0, 60)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = window.Frame

    local tabLayout = Instance.new("UIListLayout")
    tabLayout.FillDirection = Enum.FillDirection.Horizontal
    tabLayout.SortOrder = Enum.SortOrder.LayoutOrder
    tabLayout.Padding = UDim.new(0, 6)
    tabLayout.Parent = tabBar

    local tabPadding = Instance.new("UIPadding")
    tabPadding.PaddingLeft = UDim.new(0, 12)
    tabPadding.PaddingRight = UDim.new(0, 12)
    tabPadding.Parent = tabBar

    window.Content = Instance.new("Frame")
    window.Content.Size = UDim2.new(1, 0, 1, -110)
    window.Content.Position = UDim2.new(0, 0, 0, 110)
    window.Content.BackgroundTransparency = 1
    window.Content.Parent = window.Frame

    -- DRAG
    local UserInputService = game:GetService("UserInputService")
    local dragging, dragStart, frameStart
    titleBar.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            frameStart = window.Frame.Position
        end
    end)
    titleBar.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = false
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            local delta = input.Position - dragStart
            window.Frame.Position = UDim2.new(frameStart.X.Scale, frameStart.X.Offset + delta.X, frameStart.Y.Scale, frameStart.Y.Offset + delta.Y)
        end
    end)

    -- CreateTab (mesmo de antes, só mais bonito)
    function window:CreateTab(tabName, icon)
        local tabButton = Instance.new("TextButton")
        tabButton.Size = UDim2.new(0, 120, 0, 42)
        tabButton.BackgroundColor3 = InfinityUI.Theme.Button
        tabButton.Text = (icon and icon .. "  " or "") .. tabName
        tabButton.TextColor3 = InfinityUI.Theme.Text
        tabButton.TextSize = 16
        tabButton.Font = Enum.Font.GothamSemibold
        tabButton.Parent = tabBar

        local tabCorner = Instance.new("UICorner")
        tabCorner.CornerRadius = UDim.new(0, 12)
        tabCorner.Parent = tabButton

        local tabContent = Instance.new("ScrollingFrame")
        tabContent.Size = UDim2.new(1, 0, 1, 0)
        tabContent.BackgroundTransparency = 1
        tabContent.ScrollBarThickness = 5
        tabContent.ScrollBarImageColor3 = InfinityUI.Theme.Accent
        tabContent.AutomaticCanvasSize = Enum.AutomaticSize.Y
        tabContent.Visible = false
        tabContent.Parent = window.Content

        local list = Instance.new("UIListLayout")
        list.SortOrder = Enum.SortOrder.LayoutOrder
        list.Padding = UDim.new(0, 10)
        list.Parent = tabContent

        local pad = Instance.new("UIPadding")
        pad.PaddingLeft = UDim.new(0, 15)
        pad.PaddingRight = UDim.new(0, 15)
        pad.PaddingTop = UDim.new(0, 15)
        pad.PaddingBottom = UDim.new(0, 15)
        pad.Parent = tabContent

        local tab = {Button = tabButton, Content = tabContent}

        tabButton.MouseButton1Click:Connect(function()
            for _, t in pairs(window.Tabs) do
                t.Content.Visible = false
                TweenService:Create(t.Button, TweenInfo.new(0.2), {BackgroundColor3 = InfinityUI.Theme.Button}):Play()
            end
            tab.Content.Visible = true
            TweenService:Create(tabButton, TweenInfo.new(0.2), {BackgroundColor3 = InfinityUI.Theme.Accent}):Play()
        end)

        table.insert(window.Tabs, tab)

        if #window.Tabs == 1 then
            tab.Content.Visible = true
            tabButton.BackgroundColor3 = InfinityUI.Theme.Accent
        end

        -- Métodos (AddButton, AddToggle, AddSlider, AddLabel) iguais aos anteriores
        function tab:AddButton(text, callback)
            local btn = Instance.new("TextButton")
            btn.Size = UDim2.new(1, 0, 0, 55)
            btn.BackgroundColor3 = InfinityUI.Theme.Button
            btn.Text = text
            btn.TextColor3 = InfinityUI.Theme.Text
            btn.TextSize = 17
            btn.Font = Enum.Font.GothamSemibold
            btn.Parent = tabContent

            local c = Instance.new("UICorner")
            c.CornerRadius = UDim.new(0, 12)
            c.Parent = btn

            btn.MouseButton1Click:Connect(function()
                if callback then callback() end
            end)
            return btn
        end

        function tab:AddToggle(text, default, callback)
            local frame = Instance.new("Frame")
            frame.Size = UDim2.new(1, 0, 0, 50)
            frame.BackgroundTransparency = 1
            frame.Parent = tabContent

            local label = Instance.new("TextLabel")
            label.Size = UDim2.new(0.75, 0, 1, 0)
            label.BackgroundTransparency = 1
            label.Text = text
            label.TextColor3 = InfinityUI.Theme.Text
            label.TextSize = 17
            label.Font = Enum.Font.GothamSemibold
            label.TextXAlignment = Enum.TextXAlignment.Left
            label.Parent = frame

            local toggle = Instance.new("TextButton")
            toggle.Size = UDim2.new(0, 65, 0, 32)
            toggle.Position = UDim2.new(1, -75, 0.5, -16)
            toggle.BackgroundColor3 = default and InfinityUI.Theme.Accent or Color3.fromRGB(40, 40, 50)
            toggle.Text = ""
            toggle.Parent = frame

            local tc = Instance.new("UICorner")
            tc.CornerRadius = UDim.new(1, 0)
            tc.Parent = toggle

            local state = default or false
            toggle.MouseButton1Click:Connect(function()
                state = not state
                TweenService:Create(toggle, TweenInfo.new(0.25), {BackgroundColor3 = state and InfinityUI.Theme.Accent or Color3.fromRGB(40, 40, 50)}):Play()
                if callback then callback(state) end
            end)
            return {GetState = function() return state end}
        end

        function tab:AddSlider(text, min, max, default, callback)
            -- (mesmo slider bonito de antes)
            local frame = Instance.new("Frame")
            frame.Size = UDim2.new(1, 0, 0, 75)
            frame.BackgroundTransparency = 1
            frame.Parent = tabContent

            local label = Instance.new("TextLabel")
            label.Size = UDim2.new(1, 0, 0, 25)
            label.BackgroundTransparency = 1
            label.Text = text .. ": " .. (default or min)
            label.TextColor3 = InfinityUI.Theme.Text
            label.TextSize = 16
            label.Font = Enum.Font.GothamSemibold
            label.Parent = frame

            local track = Instance.new("Frame")
            track.Size = UDim2.new(1, 0, 0, 12)
            track.Position = UDim2.new(0, 0, 0, 38)
            track.BackgroundColor3 = InfinityUI.Theme.SliderTrack
            track.Parent = frame

            local tc = Instance.new("UICorner")
            tc.CornerRadius = UDim.new(1, 0)
            tc.Parent = track

            local fill = Instance.new("Frame")
            fill.Size = UDim2.new(0, 0, 1, 0)
            fill.BackgroundColor3 = InfinityUI.Theme.Accent
            fill.Parent = track
            local fc = Instance.new("UICorner")
            fc.CornerRadius = UDim.new(1, 0)
            fc.Parent = fill

            local value = default or min
            local function update() 
                local p = (value - min) / (max - min)
                fill.Size = UDim2.new(p, 0, 1, 0)
                label.Text = text .. ": " .. math.floor(value)
            end
            update()

            local dragging = false
            track.InputBegan:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                    dragging = true
                    local conn = UserInputService.InputChanged:Connect(function(ch)
                        if dragging and (ch.UserInputType == Enum.UserInputType.MouseMovement or ch.UserInputType == Enum.UserInputType.Touch) then
                            local percent = math.clamp((ch.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                            value = min + (max - min) * percent
                            update()
                            if callback then callback(value) end
                        end
                    end)
                    input.Changed:Connect(function() if input.UserInputState == Enum.UserInputState.End then dragging = false conn:Disconnect() end end)
                end
            end)
            return {GetValue = function() return value end}
        end

        function tab:AddLabel(text)
            local label = Instance.new("TextLabel")
            label.Size = UDim2.new(1, 0, 0, 40)
            label.BackgroundTransparency = 1
            label.Text = text
            label.TextColor3 = InfinityUI.Theme.Text
            label.TextSize = 16
            label.Font = Enum.Font.Gotham
            label.Parent = tabContent
            return label
        end

        return tab
    end

    table.insert(self.Windows, window)
    return window
end

return InfinityUI
